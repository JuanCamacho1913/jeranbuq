/** Duration in minutes of a single appointment slot. */
export const DEFAULT_SLOT_MINUTES = 30;

/**
 * Hours before the appointment start time within which cancellations are
 * no longer allowed without penalty.
 */
export const CANCEL_HOURS = 2;

/** Default phone country code (Colombia). */
export const DEFAULT_COUNTRY_CODE = "+57";
