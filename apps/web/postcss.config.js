/**
 * PostCSS config for Tailwind CSS 4.
 * Tailwind v4 uses @tailwindcss/postcss instead of a standalone plugin.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
